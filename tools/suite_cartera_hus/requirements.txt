pandas>=1.3
openpyxl>=3.0
python-docx>=0.8.11
pymupdf>=1.24
# Conversiones PDF -> Office (offline; no requieren LibreOffice)
pdf2docx>=0.5.6
pdfplumber>=0.10
python-pptx>=0.6.21
# Office -> PDF y PDF/A requieren LibreOffice (aparte): https://es.libreoffice.org
#
# Correos .msg de relacion de pagos -> Excel (nucleo/msg_tools.py):
# NO instalar con "pip install -r requirements.txt" tal cual, porque
# extract-msg trae como dependencia obligatoria "red-black-tree-mod", cuyo
# instalador heredado falla en cualquier setuptools moderno (Windows incluido).
# Esa pieza solo la usa extract-msg para RE-ESCRIBIR .msg; esta Suite solo los
# LEE. Instalar en dos pasos (esto ya lo hace INICIAR_SUITE.bat solo):
#   pip install --no-deps extract-msg
#   pip install olefile beautifulsoup4 compressed-rtf ebcdic RTFDE tzlocal
