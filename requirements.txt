# Motor Glosas HUS — dependencias con versiones fijadas
# Actualizar periódicamente y correr pruebas tras cada actualización

fastapi==0.115.6
uvicorn[standard]==0.34.0
groq==0.13.1
PyPDF2==3.0.1
python-multipart==0.0.20
python-dotenv==1.0.1
jinja2==3.1.5
reportlab==4.2.5
pdfplumber==0.11.5
openpyxl==3.1.5
sqlalchemy==2.0.37
python-jose[cryptography]==3.3.0
bcrypt==4.0.1
# passlib[bcrypt]==1.7.4  # ELIMINADO: incompatible con bcrypt>=4.0, usa auth.py con bcrypt directo
psycopg2-binary==2.9.10
httpx>=0.27.0,<0.29.0
pydantic-settings>=2.2.0,<3.0.0
pydantic[email]>=2.0.0,<3.0.0
slowapi==0.1.9
cachetools==5.5.0
alembic==1.14.0
# Mako llega con alembic. La versión 1.4.0 (publicada el 04-08-2026) se
# empaquetó por error con una carpeta `tools/` propia que se instala junto
# a las librerías y tapa la `tools/` del proyecto: el CI murió con
# «No module named 'tools._dinero'». El repo ya se blindó (tools/__init__.py),
# pero mientras el error siga arriba no vale la pena traer esa basura.
# Cuando Mako publique el arreglo, quitar este tope.
Mako>=1.3.0,<1.4.0
pyotp==2.9.0
qrcode[pil]==8.2
cryptography>=42.0.0
sentry-sdk[fastapi]==2.19.2
# PostHog (product analytics) — tracking de eventos server-side para
# ver qué gestores usan qué features y dónde se traban. Free tier:
# 1M eventos/mes. https://posthog.com/pricing
posthog==3.7.4
feedparser==6.0.11
pywebpush==2.0.0
psutil==6.1.1
pytz==2024.2
# Base de datos de zonas horarias para zoneinfo. En Linux/Docker la trae
# el sistema operativo; en Windows (modo sin Docker del PC del hospital)
# no existe y ZoneInfo("America/Bogota") revienta sin este paquete.
tzdata==2026.3

# 03-09-2026 (V2, Pilar 3) — ubicar el FOLIO real de un soporte dentro del
# PDF adjunto. Estaba solo en requirements-dev.txt; el motor lo necesita en
# producción para que el dictamen cite «folio 4» y no «está adjunto».
pymupdf==1.28.2
